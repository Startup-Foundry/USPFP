# Deploy Runbook

## Environment inventory
List required environment variables and where they are set (Vercel/Supabase/Cloudflare/etc.).

## Deploy targets
- Frontend: Vercel (if applicable)
- Docker: only if enabled (service/worker)
