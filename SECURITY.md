# Security

- Report security issues privately where possible.
- Dependency audits run via GitHub Actions (best-effort baseline).
- Secrets must never be committed; use environment variables and secret stores.
