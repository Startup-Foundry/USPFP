# Contributing

- Use the branch format: `agent/<agent>/<type>/<issue>-<slug>`
- Open a PR, ensure CI is green, and link the issue (`Closes #`).
- Update docs/runbooks when deploy/runtime behavior changes.
